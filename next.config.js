/** @type {import('next').NextConfig} */
const nextConfig = {
  experimental: {
    // Elimina esta línea si no usas características experimentales.
    // appDir: true,
  },
  reactStrictMode: true,
};

module.exports = nextConfig;
